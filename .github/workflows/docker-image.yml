name: Build and Push Docker Image

on:
  push:
    branches:
      - 'dev'
    tags:
      - 'v*'        # z.B. v1.2.3
  workflow_dispatch: {}

# Für GHCR-Login via GITHUB_TOKEN
permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      # 1) Repo bereitstellen
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) pnpm + Cache (falls du außerhalb von Docker installierst)
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Get pnpm store directory
        id: pnpm-cache
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 3) Docker Build-Setup
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4) Registries Logins
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 5) Tags/Labels
      - name: Extract metadata for dev
        id: meta-dev
        if: github.ref == 'refs/heads/dev'
        uses: docker/metadata-action@v5
        with:
          images: |
            butti/sparpreis-guru
            ghcr.io/${{ github.repository_owner }}/sparpreis-guru
          flavor: |
            latest=false
          tags: |
            type=ref,event=branch
            type=sha,format=long,prefix=sha-
      
      - name: Extract metadata for release
        id: meta-release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/metadata-action@v5
        with:
          images: |
            butti/sparpreis-guru
            ghcr.io/${{ github.repository_owner }}/sparpreis-guru
          flavor: |
            latest=true
          tags: |
            type=semver,pattern={{version}},prefix=v
            type=semver,pattern={{major}}.{{minor}},prefix=v
            type=semver,pattern={{major}},prefix=v

      # 6) Build & Push
      - name: Build and push Docker images
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          platforms: linux/amd64
          tags: ${{ steps.meta-release.outputs.tags || steps.meta-dev.outputs.tags }}
          labels: ${{ steps.meta-release.outputs.labels || steps.meta-dev.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
